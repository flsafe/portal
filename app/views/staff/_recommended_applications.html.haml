.recommended-students{id: 'recommended-applications'}
  .panel.panel-default
    .panel-body
      %span.title
        Recommended
      %hr
      %ol
      - @recommended_applications.each do |application|
        %li{id: "student-id-#{application.student.id}"}
          = application.student.name
