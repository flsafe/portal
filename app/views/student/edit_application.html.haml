= render partial: 'tabs', locals: {active_tab: {applications: true}}

%ol.breadcrumb
  %li
    = link_to "Applications", student_applications_path
  %li.active
    = @opportunity.title

= render partial: 'applications/form'

%hr

= render partial: 'employer/opportunities/description'